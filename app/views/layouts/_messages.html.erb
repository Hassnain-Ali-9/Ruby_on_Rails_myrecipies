<div class="row">
	<div class="col-md-8 offset-md-2">
		<% flash.each do |name, msg| %>
		<div class="alert alert-<%=name%> alert-dismissible role="alert">
			<%=msg%>
			<a href="#" class="close text-right" data-bs-dismiss="alert" aria-label="Close">&times;</a>
		</div>
		<%end%>
	</div>
</div>

